export default function Player(( title, index, ))  {
    const diceEmojis = [""]

    return (
        <>
            <div>
                <h3>{title}</h3>
                <div style={{ fontsize: "60px" }}>{diceEmojis{3}}</div>
            </div>
        </>
    );
}